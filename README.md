# Instructions 
1. Open vt.el in emacs
2. `M-x eval-buffer`
3. This will give you a new buffer called `*vt-posts*`
   - Optionally enable markdown mode in this buffer
   - Optionally `M-x markdown-preview`
